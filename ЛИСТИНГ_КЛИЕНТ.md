# 📱 Листинг - Клиентская часть (Frontend)

## Главная страница с картой станций

**Файл:** `app/map/page.tsx`

---

## 🗺️ Код загрузки станций

```typescript
// Загружаем станции из API
useEffect(() => {
  const controller = new AbortController();

  const fetchStations = async (showLoader = false) => {
    try {
      if (showLoader) setIsLoadingStations(true);
      
      // Запрос к API
      const response = await fetch("/api/stations", {
        cache: "no-store",
        signal: controller.signal,
      });
      
      if (response.ok) {
        const data = await response.json();
        setStations(data);
        
        // Если есть параметр stationId в URL - открываем эту станцию
        const paramId = searchParams.get("stationId");
        if (paramId && !autoOpenedRef.current) {
          const target = data.find((s: Station) => s.id === paramId);
          if (target) {
            autoOpenedRef.current = true;
            setFromBookingStation(paramId);
            openStationSheet(target);
          }
        }
      } else {
        console.error("Failed to fetch stations");
      }
    } catch (error: any) {
      if (error?.name !== "AbortError") {
        console.error("Error fetching stations:", error);
      }
    } finally {
      if (showLoader) setIsLoadingStations(false);
    }
  };

  if (session) {
    fetchStations(true); // первая загрузка — показываем loader

    // Polling каждые 30 сек — обновление статусов станций
    const pollingInterval = setInterval(() => fetchStations(false), 30_000);

    // Перезагружаем станции когда пользователь возвращается на страницу
    const handleVisibilityChange = () => {
      if (document.visibilityState === "visible") {
        fetchStations();
      }
    };

    document.addEventListener("visibilitychange", handleVisibilityChange);
    
    return () => {
      controller.abort();
      clearInterval(pollingInterval);
      document.removeEventListener("visibilitychange", handleVisibilityChange);
    };
  }
}, [session]);
```

---

## 📊 Структура данных Station

```typescript
interface Station {
  id: string;
  name: string;
  address: string;
  latitude: number;
  longitude: number;
  status: "available" | "busy" | "maintenance";
  maxPowerKw: number;
  pricePerKwh: number;
  connectorType: string;
  workingHours?: Record<string, { open: string; close: string }>;
  connectors?: Array<{
    id: string;
    type: string;
    powerKw: number;
    pricePerKwh: number;
    status: string;
  }>;
}
```

---

## 🔍 Фильтрация станций

```typescript
// Фильтрация станций с использованием useMemo
const filteredStations = useMemo(() => {
  let stationsToFilter = stations;

  // 1. Исключаем станции на обслуживании
  stationsToFilter = stationsToFilter.filter(
    (station) => station.status !== "maintenance",
  );

  // 2. Фильтр по совместимости с активным автомобилем
  if (showOnlyCompatible && activeVehicle) {
    stationsToFilter = stationsToFilter.filter((station) => {
      // Проверяем, есть ли у станции коннектор, совместимый с автомобилем
      if (station.connectors && station.connectors.length > 0) {
        return station.connectors.some(
          (c) => c.type === activeVehicle.connectorType
        );
      }
      return station.connectorType === activeVehicle.connectorType;
    });
  }

  // 3. Фильтр по поисковому запросу
  if (searchQuery.trim()) {
    const query = searchQuery.toLowerCase();
    stationsToFilter = stationsToFilter.filter((station) => {
      const matchesName = station.name.toLowerCase().includes(query);
      const matchesAddress = station.address.toLowerCase().includes(query);
      return matchesName || matchesAddress;
    });
  }

  // 4. Фильтр по типу разъема
  if (filters.connectorType.length > 0) {
    stationsToFilter = stationsToFilter.filter((station) => {
      // Проверяем основной тип разъема станции
      if (filters.connectorType.includes(station.connectorType)) {
        return true;
      }
      // Или проверяем коннекторы
      if (station.connectors && station.connectors.length > 0) {
        return station.connectors.some((c) =>
          filters.connectorType.includes(c.type)
        );
      }
      return false;
    });
  }

  // 5. Фильтр по мощности
  stationsToFilter = stationsToFilter.filter((station) => {
    return (
      station.maxPowerKw >= filters.minPower &&
      station.maxPowerKw <= filters.maxPower
    );
  });

  // 6. Если есть местоположение пользователя, сортируем по расстоянию
  if (userLocation) {
    const stationsWithDistance = stationsToFilter.map((station) => {
      const distance = calculateDistance(
        userLocation[1], // lat
        userLocation[0], // lng
        station.latitude,
        station.longitude
      );
      return { ...station, distance };
    });

    return stationsWithDistance.sort((a, b) => a.distance - b.distance);
  }

  return stationsToFilter;
}, [
  stations,
  searchQuery,
  userLocation,
  filters,
  showOnlyCompatible,
  activeVehicle,
]);
```

---

## 🗺️ Отображение на карте (MapLibre GL)

```typescript
// Создаем маркеры на карте
const updateMapMarkers = () => {
  if (!map.current) return;

  // Удаляем старые маркеры
  markersRef.current.forEach((marker) => marker.remove());
  markersRef.current = [];

  // Создаем новые маркеры для каждой станции
  filteredStations.forEach((station) => {
    // Создаем HTML элемент для маркера
    const el = document.createElement("div");
    el.className = "custom-marker";
    
    // Цвет маркера в зависимости от статуса
    const statusColor = 
      station.status === "available" ? "#10b981" : 
      station.status === "busy" ? "#f59e0b" : 
      "#6b7280";
    
    el.style.backgroundColor = statusColor;
    el.style.width = "32px";
    el.style.height = "32px";
    el.style.borderRadius = "50%";
    el.style.border = "3px solid white";
    el.style.cursor = "pointer";
    el.style.boxShadow = "0 2px 8px rgba(0,0,0,0.3)";

    // Создаем маркер
    const marker = new maplibregl.Marker({ element: el })
      .setLngLat([station.longitude, station.latitude])
      .addTo(map.current!);

    // Клик по маркеру - открываем детали станции
    el.addEventListener("click", () => {
      openStationSheet(station);
    });

    markersRef.current.push(marker);
  });
};
```

---

## 📋 Режим списка (List View)

```typescript
{viewMode === "list" && (
  <div className="overflow-y-auto pb-20">
    {filteredStations.length === 0 ? (
      <div className="text-center py-12 text-gray-400">
        <MapPin size={48} className="mx-auto mb-4 opacity-50" />
        <p>Станции не найдены</p>
      </div>
    ) : (
      <div className="space-y-3 p-4">
        {filteredStations.map((station) => (
          <div
            key={station.id}
            onClick={() => openStationSheet(station)}
            className="bg-[#0f2d26] border border-emerald-500/30 rounded-xl p-4 cursor-pointer hover:border-emerald-500 transition"
          >
            {/* Название станции */}
            <div className="flex items-start justify-between mb-2">
              <h3 className="text-white font-semibold flex-1">
                {station.name}
              </h3>
              {/* Статус */}
              <div
                className={`px-2 py-1 rounded-full text-xs ${
                  station.status === "available"
                    ? "bg-emerald-500/20 text-emerald-400"
                    : station.status === "busy"
                    ? "bg-yellow-500/20 text-yellow-400"
                    : "bg-gray-500/20 text-gray-400"
                }`}
              >
                {station.status === "available"
                  ? "Доступна"
                  : station.status === "busy"
                  ? "Занята"
                  : "Обслуживание"}
              </div>
            </div>

            {/* Адрес */}
            <p className="text-gray-400 text-sm mb-3">
              {station.address}
            </p>

            {/* Характеристики */}
            <div className="flex items-center gap-4 text-sm">
              <div className="flex items-center gap-1 text-emerald-400">
                <Zap size={16} />
                <span>{station.maxPowerKw} кВт</span>
              </div>
              <div className="flex items-center gap-1 text-blue-400">
                <Plug size={16} />
                <span>{station.connectorType}</span>
              </div>
              {station.distance && (
                <div className="text-gray-400">
                  {station.distance.toFixed(1)} км
                </div>
              )}
            </div>

            {/* Коннекторы */}
            {station.connectors && station.connectors.length > 0 && (
              <div className="mt-3 flex gap-2">
                {station.connectors.map((connector) => (
                  <div
                    key={connector.id}
                    className={`px-2 py-1 rounded text-xs ${
                      connector.status === "available"
                        ? "bg-emerald-500/10 text-emerald-400"
                        : "bg-gray-500/10 text-gray-400"
                    }`}
                  >
                    {connector.type}
                  </div>
                ))}
              </div>
            )}
          </div>
        ))}
      </div>
    )}
  </div>
)}
```

---

## 🎯 Открытие деталей станции

```typescript
// Функция для открытия станции в bottom sheet
const openStationSheet = (station: Station) => {
  setSelectedStation(station);
  setActiveSessionInfo(null);
  
  // Автоматически выбираем первый доступный коннектор
  const availableConnector = station.connectors?.find(
    (c) => c.status === "available",
  );
  setSelectedConnector(availableConnector || station.connectors?.[0] || null);
  setShowStationSheet(true);
  
  // Если станция занята — загружаем данные активной сессии
  if (station.status === "busy") {
    fetch(`/api/stations/${station.id}/active-session`)
      .then((r) => r.json())
      .then((data) => {
        if (data.active) {
          setActiveSessionInfo({
            driverName: data.driverName,
            estimatedEndTime: data.estimatedEndTime,
            startTime: data.startTime,
          });
        }
      })
      .catch(() => {});
  }
};
```

---

## ⚡ Особенности реализации

### 1. **Real-time обновления**
- Polling каждые 30 секунд
- Обновление при возвращении на страницу
- Без мигания интерфейса

### 2. **Оптимизация**
- `useMemo` для фильтрации
- `AbortController` для отмены запросов
- Ленивая загрузка данных

### 3. **Фильтры**
- По типу коннектора (CCS2, CHAdeMO, Type2, GB_T)
- По мощности (20-250 кВт)
- По совместимости с автомобилем
- По поисковому запросу

### 4. **Сортировка**
- По расстоянию от пользователя
- По статусу (доступные в начале)

### 5. **Два режима отображения**
- **Карта** - MapLibre GL с маркерами
- **Список** - карточки станций

---

## 📱 Другие страницы с листингом

### 1. **История зарядок** (`app/charging/history/page.tsx`)
```typescript
const fetchHistory = async () => {
  const response = await fetch("/api/charging/history");
  if (response.ok) {
    const data = await response.json();
    setHistory(data.sessions);
  }
};
```

### 2. **Список автомобилей** (`app/vehicles/page.tsx`)
```typescript
const fetchVehicles = async () => {
  const response = await fetch("/api/vehicles");
  if (response.ok) {
    const data = await response.json();
    setVehicles(data.vehicles);
  }
};
```

### 3. **История транзакций** (`app/balance/page.tsx`)
```typescript
const fetchTransactions = async () => {
  const response = await fetch("/api/user/transactions");
  if (response.ok) {
    const data = await response.json();
    setTransactions(data);
  }
};
```

---

## 🔧 Используемые библиотеки

- **MapLibre GL JS** - интерактивная карта
- **Next.js 16** - фреймворк
- **React 19** - UI
- **NextAuth** - аутентификация
- **Lucide React** - иконки
- **react-modal-sheet** - bottom sheet

---

**Файл:** `/Users/sezim/Desktop/chargeflow/app/map/page.tsx`  
**Строк кода:** ~2500+  
**Endpoint:** `GET /api/stations`
